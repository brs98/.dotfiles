# This is a demo config to showcase some of the most important commands.
# It is meant to be changed and configured, as it is intentionally kept sparse.
# For a (much) more advanced configuration example see my dotfiles:
# https://github.com/FelixKratz/dotfiles

PLUGIN_DIR="$CONFIG_DIR/plugins"

##### Bar Appearance #####
# Configuring the general appearance of the bar.
# These are only some of the options available. For all options see:
# https://felixkratz.github.io/SketchyBar/config/bar
# If you are looking for other colors, see the color picker:
# https://felixkratz.github.io/SketchyBar/config/tricks#color-picker

sketchybar --bar position=top height=40 blur_radius=30 color=0x00000000 \
                 margin=-7

##### Changing Defaults #####
# We now change some default values, which are applied to all further items.
# For a full list of all available item properties see:
# https://felixkratz.github.io/SketchyBar/config/items

default=(
  padding_left=5
  padding_right=5
  icon.font="Hack Nerd Font:Bold:16.0"
  label.font="Hack Nerd Font:Bold:14.0"
  icon.color=0x99cba6f7
  label.color=0xffffffff
  icon.padding_left=4
  icon.padding_right=4
  label.padding_left=4
  label.padding_right=4
)
sketchybar --default "${default[@]}"

##### Adding Left Items #####
# Adding the front app icon on the left side
sketchybar --add item front_app left \
           --set front_app script="$PLUGIN_DIR/front_app.sh" \
           --subscribe front_app front_app_switched

##### Adding Center Items #####
# Create separate datetime items for different display scenarios
# Multi-display: datetime in center on primary display only when external connected
sketchybar --add item datetime_center center \
           --set datetime_center update_freq=10 icon= script="$PLUGIN_DIR/clock.sh"

# Multi-display: datetime on right side of external display  
sketchybar --add item datetime_right right \
           --set datetime_right update_freq=10 icon= script="$PLUGIN_DIR/clock.sh" display=2

# Single display: datetime on right side when no external monitor
sketchybar --add item datetime_single right \
           --set datetime_single update_freq=10 icon= script="$PLUGIN_DIR/clock.sh"

##### Adding Right Items #####
# In the same way as the left items we can add items to the right side.
# Additional position (e.g. center) are available, see:
# https://felixkratz.github.io/SketchyBar/config/items#adding-items-to-sketchybar

# Some items refresh on a fixed cycle, e.g. the clock runs its script once
# every 10s. Other items respond to events they subscribe to, e.g. the
# volume.sh script is only executed once an actual change in system audio
# volume is registered. More info about the event system can be found here:
# https://felixkratz.github.io/SketchyBar/config/events

sketchybar --add item volume right \
           --set volume script="$PLUGIN_DIR/volume.sh" \
           --subscribe volume volume_change \
           --add item battery right \
           --set battery update_freq=120 script="$PLUGIN_DIR/battery.sh" \
           --subscribe battery system_woke power_source_change



##### Adding Brackets for Background Containers #####
# Left bracket for front_app with rounded background
sketchybar --add bracket left_bracket front_app \
           --set left_bracket background.color=0x991e1e2e \
                              background.corner_radius=8 \
                              background.height=32 \
                              background.drawing=on \

# Create brackets with display-specific behavior
# Center bracket: only show on primary display when external connected
sketchybar --add bracket center_bracket datetime_center \
           --set center_bracket background.color=0x991e1e2e \
                                background.corner_radius=8 \
                                background.height=32 \
                                background.drawing=on \
                                display=1

# Right bracket on primary display: only volume and battery when external connected
sketchybar --add bracket right_bracket_main volume battery \
           --set right_bracket_main background.color=0x991e1e2e \
                                    background.corner_radius=8 \
                                    background.height=32 \
                                    background.drawing=on \
                                    display=1

# Right bracket on external displays: includes datetime + volume + battery
sketchybar --add bracket right_bracket_ext datetime_right volume battery \
           --set right_bracket_ext background.color=0x991e1e2e \
                                   background.corner_radius=8 \
                                   background.height=32 \
                                   background.drawing=on \
                                   display=2

# Single display bracket: combines all items on right when no external monitor  
sketchybar --add bracket right_bracket_single datetime_single volume battery \
           --set right_bracket_single background.color=0x991e1e2e \
                                      background.corner_radius=8 \
                                      background.height=32 \
                                      background.drawing=off

# Add display manager to handle display changes automatically
sketchybar --add item display_manager left \
           --set display_manager drawing=off \
                                 script="$PLUGIN_DIR/display_manager.sh" \
           --subscribe display_manager display_change

##### Force all scripts to run the first time (never do this in a script) #####
sketchybar --update
