#compdef new

# Zsh completion for the 'new' command
_new() {
    local context state line
    typeset -A opt_args

    _arguments -C \
        '1:worktree name:' \
        '--cwd[specify working directory]:directory:_directories' \
        && return 0
}

_new "$@"